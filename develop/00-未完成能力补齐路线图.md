# 未完成能力补齐路线图

## 对齐信息

- 对齐基线：main@f1f98c884be1538c06acff265f2904745175be96
- 实现状态：已落地
- 证据路径：`develop/`、`src/`、`scripts/`、`.github/workflows/ci.yml`
- 最后更新：2026-02-17

## 1. 说明

本路线图用于承接 `develop/` 中“已落地（仓库范围）”能力的后续增强项，把文档差异转为可执行工程任务。

当前推进策略：
1. 先稳住单机生产 SLA 基线。
2. 再提升多机主备演练覆盖与自动化程度。
3. 最后推进更大规模的容量与容灾治理。

## 2. 优先级与迭代顺序

1. `P0` 文档与门禁一致性收敛。
2. `P1` 策略/回测/执行链路增强。
3. `P2` 运维演练自动化与证据治理。
4. `P3` 多机主备和跨区域容灾增强。

推荐顺序：`P0 -> Epic A -> Epic B -> Epic C -> Epic D`。

## 3. 当前批次状态

- 当前 Sprint：`D12`（双周）
- 状态：`done`
- 本期完成：
  1. develop 文档统一到纯 C++ 口径。
  2. 关键文档路径与命令对齐现有仓库资产。
  3. 运行与验收入口统一到 C++ CLI + shell。

## 4. Epic A：研究与回测能力强化

- 状态：`done`（MVP）

### 当前已实现
- `backtest_cli`、`factor_eval_cli`、`backtest_benchmark_cli`。
- `backtest_consistency_cli` 与一致性门禁脚本。
- 标准化报告输出到 `docs/results/`。

### 下一步增强
- 多场景参数搜索自动化。
- 研究结果与生产配置映射模板。
- 基准样本覆盖扩展。

## 5. Epic B：风控与执行增强

- 状态：`done`

### 当前已实现
- `RiskPolicyEngine` 规则化决策。
- `ExecutionPlanner` 执行模式与节流反馈。
- `OrderStateMachine` 生命周期约束。

### 下一步增强
- 更细粒度规则组配置。
- 执行策略效果评估与审计字段增强。

## 6. Epic C：可观测与演练闭环

- 状态：`done`

### 当前已实现
- `reconnect_evidence_cli`、`ops_health_report_cli`、`ops_alert_report_cli`。
- `run_preprod_rehearsal_gate.sh` 预发全链路演练。
- 证据文件统一落地与归档。

### 下一步增强
- 告警降噪策略。
- 关键指标阈值按环境分层。

## 7. Epic D：部署与容灾增强

- 状态：`done`（仓库范围）

### 当前已实现
- `bootstrap_prodlike.sh`、`bootstrap_prodlike_multi_host.sh`。
- `ctp_cutover_orchestrator_cli` 切换与回滚编排。
- `run_v3_acceptance.sh` 验收脚本。

### 下一步增强
- 多机主备常态化演练。
- 灾备恢复时间目标持续压缩。

## 8. 里程碑出口条件

1. 每个 Epic 完成时同步更新对应 `develop/` 文档。
2. “已落地”声明必须附真实证据路径与可执行命令。
3. 所有门禁在 CI 与本地可重复通过。
