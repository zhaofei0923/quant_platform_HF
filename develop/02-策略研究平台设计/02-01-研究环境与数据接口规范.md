# 研究环境与数据接口规范

## 对齐信息

- 对齐基线：main@17f0f8c957a4c0f95ebb054725f04b21e0e6861b
- 实现状态：部分落地
- 证据路径：`python/quant_hft/data_pipeline/`、`scripts/data_pipeline/run_pipeline.py`、`python/quant_hft/backtest/replay.py`
- 最后更新：2026-02-11

## 当前实现边界

当前仓库仅实现研究相关的最小能力：
- 回测回放基础能力（`quant_hft.backtest`）。
- 数据管道最小进程（`quant_hft.data_pipeline` + CLI）。
- 统一合约对象（`python/quant_hft/contracts.py`）。
- 回测可选 `StateSnapshot7D` 派发（`emit_state_snapshots`），用于研究侧与实盘状态合约对齐。

完整研究平台（统一特征仓、任务编排、实验管理）尚未落地。

## 已落地接口（可直接使用）

1. 回放入口：`scripts/backtest/replay_csv.py`
2. 数据管道入口：`scripts/data_pipeline/run_pipeline.py`
3. 研究侧可复用对象：`SignalIntent`、`OrderEvent`、`StateSnapshot7D`
4. 回测规范对象：`BacktestRunSpec`、`BacktestRunResult`（含输入/数据签名与数据概览）
5. 报告工件输出：`--report-json`、`--report-md`

## 规划内容（未落地）

1. 研究数据分层标准（raw/feature/label）与版本管理。
2. 实验追踪平台化（跨任务检索、聚合分析）能力。
3. 与实盘配置同构的研究配置管理机制。

## 落地依赖条件

- 明确样本数据来源与脱敏规则。
- 冻结研究输出指标定义，避免回测口径漂移。
- 建立研究任务调度与资源配额策略。

## 进入实现阶段触发条件

- 形成最小 MVP 需求清单（输入/输出/验收）。
- 具备可复现实验样例并纳入 CI smoke。
- 对应文档状态从“部分落地”变更为“已落地”时提供证据路径。
