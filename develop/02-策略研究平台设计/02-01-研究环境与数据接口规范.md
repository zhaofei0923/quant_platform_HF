# 研究环境与数据接口规范

## 对齐信息

- 对齐基线：main@f1f98c884be1538c06acff265f2904745175be96
- 实现状态：已落地
- 证据路径：`src/apps/backtest_cli_main.cpp`、`src/apps/factor_eval_cli_main.cpp`、`include/quant_hft/backtest/engine.h`
- 最后更新：2026-02-17

## 当前实现边界

当前仓库已落地研究侧最小闭环：
- 回测回放：`backtest_cli`。
- 因子评估：`factor_eval_cli`。
- 基准评测：`backtest_benchmark_cli`。
- 报告产出：JSON/Markdown 统一格式，结果落地到 `docs/results/`。

## 已落地接口（可直接使用）

1. 回放入口：`./build/backtest_cli`
2. 因子评估入口：`./build/factor_eval_cli`
3. 性能基准入口：`./build/backtest_benchmark_cli`
4. 契约对象：`SignalIntent`、`OrderEvent`、`StateSnapshot7D`
5. 报告工件：`--output_json`、`--output_md`

## 能力扩展与当前实现

1. 研究数据分层与版本治理。
2. 多场景参数搜索与批量比较。
3. 研究结果到实盘配置的自动映射。

## 落地依赖条件

- 样本数据来源与脱敏规则明确。
- 指标口径冻结，避免回测漂移。
- 研究任务执行资源与窗口明确。
