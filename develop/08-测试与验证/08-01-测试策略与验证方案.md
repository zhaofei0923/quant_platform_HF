# 验证分层

在线CI验证配置和迁移、执行、独立策略预算、实际今昨库存、原子成交、WAL恢复、CTP查询水位与只读监控。共享包CI验证自研指标、状态和纯规则；研究CI验证Tick/Bar回放、撮合、数据QA、优化和报告。

账本必须覆盖同品种不同成本、相反方向、B平今经济仓却扣账户昨仓、并发冻结、部分/迟到/重复成交、撤单请求拒绝、恢复和单实例退出。新旧Tick对比必须有实际模拟成交，不能使用空结果作为一致性证据。

外部PostgreSQL、真实CTP连接、EDB全量覆盖和连续五个交易日SimNow证据分别记录。SKIP和数据缺口保持显式，不计入对应验收完成。当前结果见[验证记录](../../docs/results/three_project_split_validation.md)。
