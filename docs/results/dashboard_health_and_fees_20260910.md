# 看板行情链路过期与手续费核验（2026-09-10）

## 结论

09-10 10:06 CST 两次实时采样确认行情 Tick、账户查询和策略风险快照持续更新，核心处于 Ready。总览的“行情链路”读取独立 `health.data.pipeline`，并不直接用最新价判断。该监控在昨晚 22:35:03 后不再输出，服务连续六次失败后触发启动限速，因而总览一直过期。

直接错误为旧监控脚本第 2074 行 `decision_ts_ns: bad array subscript`，发生于 Bash 关联数组参与延迟算术计算。另确认旧检查点读取使用 `IFS=TAB read`，会折叠连续空列，导致恢复后的字段错位；真实文件的 strategy_trace 第 11、13 列为空。

手续费链路没有缺失补零：CTP `Commission` 原值进入私有账户快照，再经发布器数值白名单输出。当前交易日 20260910 的券商返回值为 0，公开交易记录无当日成交；现有 hc2701 空单的开仓记录属于 20260909。此指标为当前交易日累计手续费，并非历史累计或持仓全部成本。未篡改该值，也未从旧记录估算替代券商字段。

## 修复与验证

- 延迟计算先从关联数组提取并验证数字，再进行标量算术；缺失事件键不会索引数组。
- 检查点逐列保留空值，验证记录结构和数字范围；损坏记录跳过并明确保持 degraded，继续发布心跳。
- 启用函数内错误追踪，后续故障可定位行号。
- 完整 `SimnowSupervisorScriptTest` 39/39 通过，覆盖真实格式 trace ID、两次空列恢复、五类损坏输入及缺失事件键；Shell 语法、格式、源码纯度检查通过。
- 服务器隔离目录复制原监控检查点后运行 `--once`，健康检查为 healthy。仅读取在线输入；输出均写入隔离目录，无凭据或告警外发环境。

10:14:14 CST 切换独立监控服务，未重启交易核心、监督程序、发布器、认证或 Nginx。核心 PID 3468568 和启动标识保持，二进制仍为已部署的 1.1.0。

后续约 20 秒五次采样中，pipeline 时间戳持续推进，质量 fresh，监控服务 active/running、NRestarts=0。10:15 短暂休市时 pipeline 显示 inactive/session=closed，页面已有逻辑显示“休市 / 非交易时段”；独立行情价格块当时为最后值/stale，不声称休市 Tick 仍在更新。未登录首页 302、认证页 200、受保护 JSON 401 均通过。

本次也观察到新核心 Ready 与实时风险快照，现有止盈目标仍为 `3257.2367647510764`；没有触发或发送测试平仓单，不将此观察等同于实际止盈成交验收。

## 发布与回滚证据

新监控目录：`/home/ubuntu/quant_monitor_releases/20260910-checkpoint-and-latency`。

```text
monitor script SHA256: a54425ef04732e6612e033aaad2c5e671e0772d32e31061fd1a43192cb51792f
archive SHA256: e3081cd817b23662cb983ad97c167fe34726bf9d20b232d90c8535fdcbd4365a
```

旧服务文件与监控输出备份：`/home/ubuntu/quant_monitor_backups/20260910-health-stale`。服务文件只替换监控脚本路径，原运行目录、独立锁、干净环境、权限和资源限制保留。回滚只停止该独立监控，恢复备份 unit 并重新加载；旧脚本含已知错误，恢复旧版不代表恢复可用性。无需调整交易状态或 WAL。

本地证据目录：`/home/kevin/quant_split_evidence/20260910/dashboard_health_fees`，包含原始脱敏采样、失败日志、39 项测试 XML、隔离验证、部署 PID 对比及 `live-verification.json`。
