[Unit]
Description=RETIRED Quant SimNow direct user launcher
# The direct launcher did not bind an explicit reviewed account instance. Keep this
# source unit fail-closed so copying it cannot bypass the packaged supervisor.
RefuseManualStart=yes

[Service]
Type=oneshot
ExecStart=/bin/false
